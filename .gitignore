# Build artifacts (regenerated by BPHandlers\build.bat)
BPHandlers/Output/

# Compiled / third-party UT package binaries. This repo's only source is *.uc;
# every *.u / *.uz / *.utx here is a build output or a compile input (IG+
# release files, stock GOTY packages, cylinders), never tracked.
*.u
*.uz
*.utx

# Server files dropped in for analysis (logs + config copies), not project source
*.log
UnrealTournament.ini
InstaGibPlus.ini
User.ini

# Editor / Claude Code temp files
*.tmp.*

# OS
.DS_Store
Thumbs.db
